<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.cattaka.android.learnlayout" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="net.cattaka.android.learnlayout.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MultiScrollActivity" />
        <activity android:name=".FadeInActionBarActivity" />
        <activity android:name=".PullToRefleshActivity" />
        <activity android:name=".HeaderListActivity" />
        <activity android:name=".ViewPagerActivity" />
        <activity android:name=".RecyclerViewActivity" />
        <activity android:name=".RippleEffectActivity" />
        <activity android:name=".CardviewActivity" />
        <activity android:name=".GridlayoutActivity" />
        <activity android:name=".CustomActionBarActivity" />
        <activity android:name=".HeaderedViewPagerActivity" />
    </application>

</manifest>
